// const Northwestern = require('../../../../pages/northwestern')

// // Then(/^user should see NavBar items$/, (dataTable) => {
// //   const arr = dataTable.rawTable.flat()
// //   nw.textlocatorfor6text().each((el, index) => {
// //     cy.wrap(el).should('contain', arr[index])
// //   })
// // })

// const nw = new Northwestern()
